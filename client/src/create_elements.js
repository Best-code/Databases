

export function createLayerSelect(parent){ //returns created element as child of 'parent' which is a document node

    var menuDiv = document.createElement('div');

    menuDiv.id = 'layerMenu';

    parent.appendChild(menuDiv);

    var selections = document.createElement('')


}